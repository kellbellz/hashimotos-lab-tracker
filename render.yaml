services:
  - type: web
    name: rook-online
    runtime: node
    plan: free
    rootDir: rook-online
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
